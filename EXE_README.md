# AutoRelink の使い方（exe版）

GRANBLUE FANTASY: Relink のリザルト画面を自動で進め、「再挑戦」を自動選択して
周回を続けるツールです。Python のインストールは不要で、`AutoRelink.exe` を
ダブルクリックするだけで使えます。

## 注意事項（必ずお読みください）

- 本ツールは通常のキーボード入力を OS レベルでゲームに送るだけの
  「周回補助マクロ」です。メモリ改ざんや通信の改変は行いません。
- ただし、ゲームの利用規約でマクロ/自動化ツールの使用が禁止されている
  場合があります。利用は自己責任で、必ず対象タイトルの利用規約を
  確認してください。
- オンライン協力プレイ中の使用は他プレイヤーに影響する可能性があるため、
  ソロ/オフライン周回での利用を推奨します。
- 誤動作時にすぐ止められるよう、一時停止/終了ホットキー（既定 `F8` /
  `F10`）を覚えておいてください。

## 動作環境

- Windows 10 / 11（64bit）
- GRANBLUE FANTASY: Relink（Steam 版）
- 画面表示モードは「ボーダーレスウィンドウ」を推奨（排他フルスクリーンだと
  入力が届かないことがあります）

## インストール

1. `AutoRelink.exe` を好きなフォルダに置いてください（実行すると同じ
   フォルダに `config.json` / `templates` / `assets` が自動生成されるため、
   専用のフォルダに置くことをおすすめします）。
2. ダブルクリックで起動します。
   - Windows Defender SmartScreen が「発行元不明」の警告を出すことが
     あります。「詳細情報」→「実行」で起動できます（個人開発の
     ツールで署名していないための警告で、多くの自動化ツールで
     出るものです）。
   - グローバルホットキー（`F8`/`F10` など）を安定して使うため、
     「管理者として実行」での起動を推奨します。

## 初回セットアップ（テンプレート作成）

起動したら上部の「テンプレート作成」タブで、4つの画面をあらかじめ
登録します（お使いの画面解像度・UIスケールに合わせるため、実際の画面
から撮影する必要があります）。

| 項目 | 内容 |
|---|---|
| ① 次へ表示 | リザルト画面などで「次へ」が表示されている状態 |
| ② 再挑戦する（未選択） | 報酬画面で「③ 再挑戦する」が未選択の状態 |
| ③ 確認ポップアップ「はい」 | 「リザルトの確認を完了しますか？」で「はい」がハイライトされた状態 |
| ④ 継続確認ポップアップ | 「引き続きこのクエストに挑戦しますか？」ポップアップ（一定回数ごとに表示） |

手順（各項目共通）:

1. 「キャプチャ開始」を押す
2. ゲーム画面をフォーカスし、対象の画面を実際に表示させる
3. `F9` を押す → スクリーンショットが撮影される
4. 表示された選択ウィンドウでドラッグして範囲を選び、`Enter` で確定
   （`C` でキャンセル）

範囲は「その画面が表示されているかどうかの判定」にのみ使われ、実際の
操作はキー入力なので、範囲の位置や広さは動作に影響しません。他の画面と
紛らわしくない部分を選べば十分です（②だけは「する」/「をキャンセル」の
文字が変わる部分まで含めて選んでください）。

すでに `templates` フォルダにテンプレート画像が入っている場合は、この
手順を省略してそのまま使えます。画面解像度やUIスケールが変わった場合、
または検出がうまくいかない場合だけ、該当項目を撮り直してください。

## 実行

ゲームを起動し、周回したいクエストのリザルトが出るところまで進めたら、
「実行」タブで「開始」を押してください。

- 「一時停止」/「再開」、「停止」ボタン、またはホットキー `F8`（一時停止/
  再開）・`F10`（停止）で制御できます。
- ログ欄に検出・操作の履歴が表示されます。
- 「スコア確認」ボタンで、現在の画面に対する各テンプレートの一致度を
  クリック/キー入力せずに確認できます（判定したい画面を実際に表示させた
  状態で押してください）。

ゲームウィンドウは自動でフォーカスしてから操作するので、AutoRelink の
ウィンドウにフォーカスがあっても構いません。

## 設定タブ

- `match_threshold`: テンプレート一致のしきい値。誤検出が多ければ上げる、
  検出しなければ下げる。
- `poll_interval` / `post_key_delay` / `post_click_delay`: 判定間隔や
  入力後の待機時間。
- `monitor_index`: マルチモニタ環境でゲームが表示されているモニタ番号。
- `pause_hotkey` / `stop_hotkey` / `capture_hotkey`: 各ホットキー。
- `game_window_title`: 自動フォーカスするゲームウィンドウ名（部分一致）。
  「ウィンドウ確認」ボタンで実際に何が検出されるか確認できます。
- `confirm_key`: 「次へ」「はい」の代わりに押すキー（既定 `enter`）。
- `select_up_key`: 継続確認ポップアップで「はい」に選択を移すキー
  （既定 `w`）。

設定は `config.json` に保存され、次回起動時にも引き継がれます。

## トラブルシューティング

**「次へ」などは検出（スコア表示）されるのに画面が進まない**

このゲームのリザルト/メニュー画面はマウスクリックには反応せず、
キーボード（コントローラーの決定ボタン相当）でのみ操作できるため、
本ツールはキー入力（`confirm_key`、既定 `Enter`）で操作します。それでも
進まない場合:

1. ゲーム側のキー設定で「決定」に割り当てられているキーを確認し、
   異なる場合は設定タブの `confirm_key` を変更する。
2. 設定タブの「ウィンドウ確認」で正しいウィンドウが検出されているか
   確認する。
3. ゲームの表示モードをボーダーレスウィンドウ/ウィンドウモードに変更する。

**特定のテンプレートだけ検出されない**

「スコア確認」ボタンで、判定したい画面を実際に表示させた状態でスコアを
確認してください。スコアが低い場合は、テンプレート作成タブから該当項目を
撮り直してください（範囲が広すぎる、共通部分しか含んでいない、等が典型的な
原因です）。

## アンインストール

`AutoRelink.exe` と、生成された `config.json` / `templates` / `assets`
フォルダを削除するだけです。レジストリなどへの変更は行いません。
